import styled from 'styled-components';

import Layout from './layout';

export default styled(Layout)`
    align-items: center;
    background: white;
    box-shadow: 0px 1px 10px hsla(45, 100%, 3%, 0.35), 0px 2px 4px hsla(51, 100%, 12%, 0.2);
    box-sizing: border-box;
    min-height: 64px;
    padding: 0 16px;
`